# All ports can be switched as well

# Server Configuration
SERVER_HOST=your_name
SERVER_PORT=6777

# Frontend Configuration  
FRONTEND_HOST=your_name2
FRONTEND_PORT=6778

# Database Configuration
POSTGRES_HOST=your_name3
POSTGRES_PORT=6779
POSTGRES_USER=your_name
POSTGRES_PASSWORD=your_password
POSTGRES_DB=your_db_name

# LLM Configuration (Optional)
# Set USE_LLM=true to enable LLM-enhanced features
# Note: The application works without LLM (falls back to rule-based logic)
USE_LLM=false
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-2.5-flash-lite